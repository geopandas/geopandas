version: 2
build:
  os: ubuntu-22.04
  tools:
    python: 3.11
  jobs:
    post_checkout:
      # we need the tags for versioneer to work
      - git fetch origin --depth 150
      - git fetch --tags
python:
  install:
  - requirements: doc/requirements.txt
  - method: pip
    path: .

formats: []
