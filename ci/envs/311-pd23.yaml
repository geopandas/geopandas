name: test
channels:
  - conda-forge
dependencies:
  - python=3.11
  # required
  - pandas=2.3
  - numpy=2.3 # released 3 days after pandas 2.3
  - shapely
  - pyogrio
  - pyproj
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  - requests
  - aiohttp
  - pytz
  # optional
  - matplotlib
  - mapclassify
  - folium
  - xyzservices
  - scipy
  - geopy
  - geodatasets
  # installed in tests.yaml, because not available on windows
  # - postgis
  - SQLalchemy<2.1 # test oldest support sqlalchemy version
  - psycopg2
  - libspatialite
  - geoalchemy2
  - pyarrow
  # doctest testing
  - pytest-doctestplus
