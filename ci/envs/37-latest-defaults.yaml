name: test
channels:
  - defaults
dependencies:
  - python=3.7.3
  # required
  - pandas
  - shapely
  - fiona
  - pyproj
  - geos
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - rtree
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pyarrow
  - pip:
      - geopy
      - mapclassify
