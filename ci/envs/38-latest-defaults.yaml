name: test
channels:
  - defaults
dependencies:
  - python=3.8
  # required
  - numpy
  - pandas
  - shapely
  - fiona
  - pyproj>=3
  - geos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - rtree
  - matplotlib
  #- geopy
  - SQLalchemy>2
  - libspatialite
  - pip
  - pip:
      - geopy
      - mapclassify
      - pyarrow
      - folium
      - xyzservices
