name: test
channels:
  - defaults
dependencies:
  - python=3.8
  # required
  - pandas
  - shapely
  - fiona
  - pyproj
  - geos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - rtree
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - geopy
      - mapclassify
      - pyarrow
      - folium
      - xyzservices
