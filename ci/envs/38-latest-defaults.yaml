name: test
channels:
  - defaults
dependencies:
  - python=3.8
  # required
  - numpy=1.21  # pin to < 1.23 for compat with older shapely on defaults channel
  - pandas
  - shapely
  - fiona
  - pyproj
  - geos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - rtree
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - geopy
      - mapclassify
      - pyarrow
      - folium
      - xyzservices
