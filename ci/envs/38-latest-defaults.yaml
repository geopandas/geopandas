name: test
channels:
  - defaults
dependencies:
  - python=3.10
  # required
  - numpy
  - pandas
  - shapely
  - fiona
  # - pyproj>=3
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - rtree
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - pyproj>=3 --no-binary pyproj
      - geopy
      - mapclassify
      - pyarrow
      - folium
      - xyzservices
