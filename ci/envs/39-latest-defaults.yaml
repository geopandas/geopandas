name: test
channels:
  - defaults
dependencies:
  - python=3.9
  # required
  - numpy
  - pandas
  - shapely
  # - pyogrio # comes as 0.5.1
  - pyproj>=3.3.0
  - geos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  - pytz
  # optional
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - pyogrio
      - geopy
      - mapclassify
      - pyarrow
      - folium
      - xyzservices
      - geodatasets
