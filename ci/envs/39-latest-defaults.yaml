name: test
channels:
  - defaults
dependencies:
  - python=3.9
  # required
  - numpy
  - pandas
  - shapely
  - fiona
  - pyproj>=3.3.0
  - geos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - geopy
      - mapclassify
      - pyarrow
      - folium
      - xyzservices
      - geodatasets
