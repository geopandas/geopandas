name: test
channels:
  - defaults
dependencies:
  - python=3.10
  # required
  - pandas=2.0
  - shapely
  # - pyogrio # comes as 0.5.1
  - numpy=1.24
  - pyproj>=3.4.0
  - geos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  - pytz
  # optional
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - pyogrio
      - geopy
      - mapclassify==2.5.0
      - pyarrow
      - geodatasets
