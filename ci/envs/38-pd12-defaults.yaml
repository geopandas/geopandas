name: test
channels:
  - defaults
dependencies:
  - python=3.8
  # required
  - pandas=1.2
  - shapely
  - fiona
  - numpy=<1.22
  - pyproj=3.1.0
  #- pyproj
  - geos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - rtree
  - matplotlib
  #- geopy
  - SQLalchemy>2
  - libspatialite
  - pip
  - pip:
      - geopy
      - mapclassify==2.4.0
      - pyarrow
