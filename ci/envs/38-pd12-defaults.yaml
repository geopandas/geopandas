name: test
channels:
  - defaults
dependencies:
  - python=3.8
  # required
  - pandas=1.2
  - shapely=1.8
  - fiona=1.8
  - numpy=<1.22
  # - pyproj=3.1.0
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - rtree
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - pyproj==3.1.0
      - geopy
      - mapclassify==2.4.0
      - pyarrow
