name: test
channels:
  - defaults
dependencies:
  - python=3.7
  # required
  - pandas=1.0
  - shapely
  - fiona
  - numpy=<1.19
  #- pyproj
  - geos
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - rtree
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - pyproj==3.0.1
      - geopy
      - mapclassify==2.4.0
      - pyarrow
