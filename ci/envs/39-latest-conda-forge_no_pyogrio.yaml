name: test
channels:
  - conda-forge
dependencies:
  - python=3.9
  # required
  - pandas
  - shapely
  # - pyogrio  # build with only fiona
  - libgdal
  - pyproj
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  # - fsspec  # to have one non-minimal build without fsspec
  # optional
  - matplotlib>=3.6
  - mapclassify
  - folium
  - xyzservices
  - scipy
  - geopy
  - SQLalchemy
  - libspatialite
  - pyarrow
  - fiona
  - pointpats
  - geodatasets
