name: test
channels:
  - conda-forge
dependencies:
  - python=3.8
  # required
  - pandas
  - shapely
  - fiona
  - pyproj
  - pygeos
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - rtree
  - matplotlib
  - mapclassify
  - scipy
  - geopy
  # installed in tests.yaml, because not available on windows
  # - postgis
  - SQLalchemy
  - psycopg2
  - libspatialite
  - geoalchemy2
  - pyarrow
  # doctest testing
  - pytest-doctestplus