name: test
channels:
  - conda-forge
dependencies:
  - python=3.8
  # required
  - pandas
  - shapely
  # - fiona  # build with only pyogrio
  - libgdal
  - pyproj
  - pygeos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  # - fsspec  # to have one non-minimal build without fsspec
  # optional
  - rtree
  - matplotlib
  - mapclassify
  - folium
  - xyzservices
  - scipy
  - geopy
  - SQLalchemy>2
  - libspatialite
  - pyarrow
  - pyogrio
