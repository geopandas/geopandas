name: test
channels:
  - defaults
dependencies:
  - python=3.11
  # required
  - numpy
  - pandas
  - shapely
  # - pyogrio # comes as 0.5.1
  - pyproj>=3.4.0
  - geos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - pyogrio
      - geopy
      - mapclassify
      - pyarrow
      - folium
      - xyzservices
      - geodatasets
