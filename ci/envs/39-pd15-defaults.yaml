name: test
channels:
  - defaults
dependencies:
  - python=3.9
  # required
  - pandas=1.5
  - shapely
  # - pyogrio # comes as 0.5.1
  - numpy=1.23
  - pyproj>=3.3.0
  - geos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - pyogrio
      - geopy
      - mapclassify==2.4.0
      - pyarrow
      - geodatasets
