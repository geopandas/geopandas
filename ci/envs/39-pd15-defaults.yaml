name: test
channels:
  - defaults
dependencies:
  - python=3.9
  # required
  - pandas=1.5
  - shapely
  - fiona
  - numpy=1.23
  - pyproj>=3.3.0
  - geos
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - fsspec
  # optional
  - rtree
  - matplotlib
  #- geopy
  - SQLalchemy
  - libspatialite
  - pip
  - pip:
      - geopy
      - mapclassify==2.4.0
      - pyarrow
      - geodatasets
